{
    "$schema": "../ssx/spec/ssx.config.schema.json",
    "overrides": [
        {
            "comment": "Compat shims mirror other sites' CSS verbatim so their markup keeps rendering. They live under 32-utilities/ for build order, but they are not utilities — judging them as single-purpose helpers buries every real finding. ss.overrides is what they actually are: third-party CSS held at arm's length.",
            "files": [
                "src/scss/32-utilities/_scapepress-parity.scss",
                "src/scss/32-utilities/_legacy-compat.scss",
                "src/scss/32-utilities/_legacy-components-compat.scss"
            ],
            "layer": "ss.overrides"
        }
    ]
}
