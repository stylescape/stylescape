{# 
    Copyright 2025 Scape Agency BV
#}

{# 
    Image Map (<map>)
    ===========================================================================

    Defines an image map, which is an image with clickable areas.

    Variables:
    - `map_name`: The name of the image map. Defaults to "map".
    - `areas`: A list of dictionaries, each containing attributes for an <area> element. Defaults to a single default area.

    Notes:
    - The `| e` filter is used to escape any special characters in the variables to prevent XSS attacks.
    - Each dictionary in `areas` should contain `shape`, `coords`, `href`, `alt`, `target`, and `rel` keys.

    Links:
    - https://developer.mozilla.org/en-US/docs/Web/HTML/Element/map
    - https://www.w3schools.com/tags/tag_map.asp

#}

<map name="{{ map_name | default('map') | e }}">
    {% for area in areas | default([{'shape': 'rect', 'coords': '0,0,100,100', 'href': '#', 'alt': 'Default area', 'target': '_blank', 'rel': 'noopener noreferrer'}]) %}
        <area 
            shape="{{ area.shape | e }}" 
            coords="{{ area.coords | e }}" 
            href="{{ area.href | e }}" 
            alt="{{ area.alt | e }}" 
            target="{{ area.target | e }}" 
            rel="{{ area.rel | e }}"
        >
    {% endfor %}
</map>
