{# 
    Copyright 2025 Scape Agency BV
#}

{# 
    HTML Document (<html>)
    ===========================================================================

    The root element of an HTML document.

    Variables:
    - `html_lang`: The language of the document. Defaults to "en".
    - `html_content`: The content to be included inside the `<html>` element.
      Defaults to a basic HTML structure.

    Notes:
    - The `| e` filter is used to escape any special characters in the variables to prevent XSS attacks.
    - The `| safe` filter is used to include HTML content safely.

    Links:
    - https://developer.mozilla.org/en-US/docs/Web/HTML/Element/html
    - https://www.w3schools.com/tags/tag_html.asp

#}

<html lang="{{ html_lang | default('en') | e }}">
    {{ html_content | default('<head><title>Default Title</title></head><body>Default Content</body>') | safe }}
</html>


<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
</head>
<body>
    {% block body %}{% endblock %}
</body>
</html>
